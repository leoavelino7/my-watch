import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  dayContainer: {
    width: '100%',
    height: 100,
  },
  sumMoon: {
    width: 30,
    height: 30,

    borderRadius: 100,
  },
});
